{
  "name": "mermaid-live-viewer",
  "private": true,
  "type": "module",
  "description": "Deps for the offline .mmd validator (validate.mjs). The viewer itself (server.js) runs on bun with no deps.",
  "dependencies": {
    "jsdom": "^29.1.1",
    "mermaid": "^11.15.0"
  }
}
